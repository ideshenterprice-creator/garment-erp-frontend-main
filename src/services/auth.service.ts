// Placeholder — auth API methods will be implemented later
export const authService = {
  login: async (): Promise<void> => {},
  logout: async (): Promise<void> => {},
  refresh: async (): Promise<void> => {},
  acceptInvite: async (): Promise<void> => {},
};
