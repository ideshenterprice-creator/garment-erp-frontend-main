// Placeholder — production API methods will be implemented later
export const productionService = {
  getEntries: async (): Promise<void> => {},
  getBundle: async (_bundleNumber: string): Promise<void> => {
    void _bundleNumber;
  },
};
